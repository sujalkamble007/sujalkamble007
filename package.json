{
  "name": "sujal-github-profile",
  "version": "1.0.0",
  "description": "Generates an animated jet-over-contribution-grid SVG from real GitHub contribution data",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "generate": "node generate.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
